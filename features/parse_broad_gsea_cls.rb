Given /^I have a CLS file which contains$/ do |string|
  pending # express the regexp above with the code you wish you had
end

Then /^I should fetch the phenotype names RS(\d+) and RS(\d+)_(\d+)$/ do |arg1, arg2, arg3|
  pending # express the regexp above with the code you wish you had
end

